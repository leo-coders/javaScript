switch (num) {
  case num < 50:
    console.log(`less than ${num}`);
    break;
  case num > 50:
    console.log(`greater than ${num}`);
    break;
  case 50:
    console.log(`equal to ${num}`);
    break;
  default:
    console.log("janina");
    break;
}
